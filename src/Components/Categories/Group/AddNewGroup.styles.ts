import styled from 'styled-components';
import { Container } from '../../../globalStyles';

export const StyledAddNewGroup = styled(Container)`
  display: flex;
  justify-content: center;
  align-items: center;
`;
