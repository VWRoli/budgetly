import styled from 'styled-components';
import { Container } from '../../../globalStyles';

export const StyledGroup = styled(Container)`
  max-width: 500px;
`;

export const GroupTitle = styled.h2`
  font-weight: 600;
  font-size: 1.25rem;
`;
